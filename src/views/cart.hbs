
    <form class="card" action="card" method="git">

    <table >
  <tr id="th1">
    <th>Sandwish name</th>
    <th>amount</th>
    <!-- <th>Time</th> -->
    <th>Note</th>
    <th>price</th>
    <th>Delete Order</th>
  </tr>
  {{#each data}}

  <tr>
    <td>{{name}}</td>
    <td>{{amount}}</td>
    <td><input type="textarea" placeholder="EX : extra cheese in tost" name="" value=""></td>
      <td>{{price}}</td>
      <td><button id="{{id}}" type="button" name="delete" class="delete">Delete</button></td>
  </tr>
  {{/each}}
</table>

   <input id="confirm" type="submit" name="confirm" value="confirm">
    </form>
